# BACKEND RECOVERY MODE — DO NOT CONTINUE WITH FRONTEND UNTIL BACKEND IS STABLE

The previous verification correctly discovered that the backend is currently broken.

Do NOT continue implementing frontend features.

The backend must be repaired first.

---

## CURRENT ISSUE

The service layer references:

Event

Booking

EventRepository

BookingRepository

but these classes are currently missing from the repository.

This prevents successful compilation.

Do NOT work around the problem.

Fix it properly.

---

## STEP 1 — VERIFY THE ACTUAL DOMAIN MODEL

Inspect every backend package.

Determine whether:

* Event
* Booking

were intentionally renamed,

or

accidentally deleted.

Search the entire repository before restoring anything.

If equivalent entities already exist under different names, adapt the services to use those entities instead of creating duplicates.

Do NOT create duplicate domain models.

---

## STEP 2 — USE GIT HISTORY

Search Git history carefully.

Locate the last stable implementation of:

* Event.java
* Booking.java
* EventRepository.java
* BookingRepository.java

Restore them ONLY if they are genuinely the correct domain model.

If only portions are needed, restore only the necessary code.

Do not blindly copy outdated code.

---

## STEP 3 — VERIFY SERVICES

Check every service:

EventServiceImpl

BookingServiceImpl

DashboardServiceImpl

GalleryServiceImpl

MemberServiceImpl

NotificationServiceImpl

AuthServiceImpl

Ensure every injected repository actually exists.

Ensure every repository method exists.

If repository APIs changed, update the service layer.

Do not leave broken method references.

---

## STEP 4 — VERIFY CONTROLLERS

Verify:

AdminController

UserController

PublicController

AuthController

UploadController

Every controller must compile.

Every service injection must resolve.

Every DTO must exist.

---

## STEP 5 — VERIFY REPOSITORIES

Every repository referenced by services must exist.

Every custom query method must exist.

Examples include methods such as:

findByUserIdAndEventId

findByStatusOrderByCreatedAtDesc

findByUserIdOrderByCreatedAtDesc

Either:

* implement the repository methods,

or

* update the service layer to the correct repository API.

---

## STEP 6 — VERIFY ENTITIES

Verify all JPA relationships.

Ensure:

User

Event

Booking

Gallery

Member

Content

have valid mappings.

Check:

@OneToMany

@ManyToOne

@JoinColumn

cascade

fetch strategy

mappedBy

Do not leave invalid mappings.

---

## STEP 7 — BUILD THE BACKEND

Run:

mvn clean package

Fix every compilation error.

Continue rebuilding until Maven succeeds.

Do not stop after the first error.

Resolve every subsequent error.

---

## STEP 8 — START SPRING BOOT

Run the backend.

Fix:

bean creation errors

repository errors

JPA mapping errors

entity scan errors

dependency injection errors

Supabase configuration errors

Continue until Spring Boot starts successfully.

---

## STEP 9 — ONLY AFTER BACKEND IS STABLE

Resume the previous verification work:

* frontend integration
* API testing
* QR flow
* admin testing
* documentation
* production verification

Do not continue with frontend while the backend is broken.

---

## FINAL REPORT

Provide:

* Root cause of the backend breakage.
* Which files were restored.
* Which files were modified.
* Which Git commit(s) were used.
* Backend build status.
* Remaining compile errors (if any).
* Whether the backend now starts successfully.

Do not claim success until the backend compiles and starts successfully.
