The backend Maven build failed. Fix ONLY this build/dependency problem.

DO NOT redesign the project.
DO NOT remove or rewrite the membership registration feature.
DO NOT remove Cloudinary functionality.
DO NOT change the payment screenshot flow.
DO NOT change Supabase private storage.
DO NOT modify unrelated frontend/backend features.

Current Maven error:

Could not resolve dependency:

com.cloudinary:cloudinary-http5:jar:2.5.0

Maven Central cannot find:

cloudinary-http5-2.5.0

The current pom.xml incorrectly contains:

<dependency>
    <groupId>com.cloudinary</groupId>
    <artifactId>cloudinary-http5</artifactId>
    <version>2.5.0</version>
</dependency>

Replace the invalid version with a valid Maven Central version compatible with the existing Cloudinary Java integration.

Use:

com.cloudinary:cloudinary-http5:2.4.0

Then:

1. Inspect the existing Cloudinary-related Java imports and code.
2. Ensure the dependency version is compatible with the existing code.
3. Do not change working Cloudinary functionality unless required for compatibility.
4. Run:

cd backend
mvn clean package -DskipTests

5. If another compilation or dependency error appears, fix ONLY the actual error.
6. Repeat the Maven build until it passes.

Also run:

npm run build

after the backend build succeeds, but do not modify frontend code unless the build actually fails.

Finally provide:

- Exact dependency version used
- Backend build result: PASS/FAIL
- Frontend build result: PASS/FAIL
- Files modified
- Any remaining issue

Success criteria:

mvn clean package -DskipTests

must end with:

BUILD SUCCESS